import { classed } from '@tw-classed/react';

export const TodayWeatherWrapper = classed.div('bg-primary/10 rounded-md p-4');

export const TodayWeatherSection = classed.section('flex flex-col items-center justify-center gap-2');

export const TodayWeatherSubSection = classed.section('flex items-center justify-center gap-2');
